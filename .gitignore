__pycache__/
*.pyc
.venv/
.env
.ipynb_checkpoints/

# Artifacts are small for this project (≤ a few MB) so we commit them — this
# means the API/MCP teammates don't have to retrain to get a working stack.
# If artifacts ever grow past ~50MB, switch to git-lfs or a release upload.

# IDE
.vscode/
.idea/
